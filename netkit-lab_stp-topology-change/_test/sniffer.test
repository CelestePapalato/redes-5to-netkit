#!/bin/sh

# Attempt to detect the exchange of topology change notification bpdus

tcpdump -s 1518 -e -t -n -l stp > /tcpdump.log &

sleep 30

killall -9 tcpdump
grep -E "(tcn$)|(TOP_CHANGE TOP_CHANGE_ACK)" /tcpdump.log

touch /hostlab/sniffer.testdone
halt

